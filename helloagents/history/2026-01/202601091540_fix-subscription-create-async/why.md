# Why: 修复订阅创建阻塞 UI

## 问题
- 前端「保存订阅」会调用 `POST /configs/import`。
- 后端在创建配置时同步执行订阅拉取（网络请求）+ 解析 + 同步节点/FRouter。
- 当订阅服务响应慢或网络不稳定时，请求链路会明显变长，UI 容易被用户误认为“卡死/无响应”。

## 目标
- 创建订阅应快速返回，让 UI 立即关闭弹窗并恢复交互。
- 拉取/解析在后台完成，并通过订阅列表的同步时间/状态反映结果（未同步/失败/正常）。

