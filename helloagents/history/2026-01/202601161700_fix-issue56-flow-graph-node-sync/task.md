# 任务清单（轻量迭代）

> 目标: 修复 Issue #56「实际节点与走向图节点不同步」。

- [√] 前端：修复 `loadNodes()` 并发加载时的等待语义，避免走向图渲染时读取到旧 `nodesCache`。
- [√] 前端：走向图渲染缓存 key 纳入节点版本（nodes id/name 变化会触发重渲染），确保订阅“拉取节点”后走向图同步更新。
- [√] 验证：运行 `go test ./...`。

